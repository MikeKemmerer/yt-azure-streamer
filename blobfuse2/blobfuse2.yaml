# Blobfuse2 configuration template
# This file is parameterized — the installer will replace STORAGE_ACCOUNT dynamically
# based on /etc/yt/nameprefix (written by ARM customData)

logging:
  type: syslog
  level: LOG_INFO

components:
  - libfuse
  - file_cache
  - attr_cache
  - azstorage

azstorage:
  type: block
  account-name: STORAGE_ACCOUNT
  container: recordings
  endpoint: https://STORAGE_ACCOUNT.blob.core.windows.net
  auth-type: msi

file_cache:
  path: /mnt/blobfuse2_cache
  timeout-sec: 120
  max-size-mb: 4096

attr_cache:
  timeout-sec: 120
